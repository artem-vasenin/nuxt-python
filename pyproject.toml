[project]
name = "nuxt-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "pydantic>=2.13.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
